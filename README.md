# Jungian Cheatsheet

## File Tree

* The LyX files under the `lyx/` directory contain the source of some tables for easy of manual editing. Some tex files under the `src/` directory are generated from these LyX files, including the `*-manifest.tex` files.
